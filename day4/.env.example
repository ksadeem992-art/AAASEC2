# LLM (same as every day)
OPENAI_API_KEY=your-openrouter-key
2
# LangSmith tracing — free account at https://smith.langchain.com
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=your-langsmith-key
LANGSMITH_PROJECT=aaasec2-day4

# Backend for the execute tool: "local" (default, YOUR machine) | "daytona" (extra)
SANDBOX_PROVIDER=local
# DAYTONA_API_KEY=            # only for the 05 extra

# MCP auth (Day 4 dev tokens — never do static tokens in production)
MCP_STUDENT_TOKEN=student-secret-token
MCP_ADMIN_TOKEN=admin-secret-token
